spring.application.name=SpringBootProject-3

# Database connection settings
spring.datasource.url=jdbc:mysql://localhost:3306/todolist_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=aniket25
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (Hibernate) settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Thymeleaf settings (optional)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# Server settings (optional)
server.port=8080

# Logging settings (optional)
logging.level.org.springframework=INFO
logging.level.com.example.todolist=DEBUG
